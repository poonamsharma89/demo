import { MypercentagePipe } from './mypercentage.pipe';

describe('MypercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new MypercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
